# 传统机器学习算法与神经网络学习总结

## 1. 各个算法的原理与使用场景

### (1) kNN分类器 (K-Nearest Neighbor)
**原理**：kNN对于测试集中的每一张图像，计算它与训练集中所有图像的距离（如 L2 欧氏距离 ），然后找出距离最近的k张图像，让它们投票决定测试图像的类别。。
**使用场景**：适合作为任务的基线模型，或者数据维度不高的情况。推理阶段耗时较长，因为每次预测都要和全体训练集计算一次距离。

### (2) 支持向量机 (SVM)
**原理**：SVM 的目标是找出一个权重矩阵，使得正确类别的得分比其他错误类别的得分至少高出一个边界值。
**使用场景**：在数据维度较高、但数据量中等时，如文本分类，泛化能力很强。预测时只需进行简单的矩阵乘法计算，推理速度比kNN快很多。

### (3) Softmax 分类器
**原理**：Softmax将模型的原始得分转化为概率分布，然后最大化正确类别的概率对数。
**使用场景**：常用于多分类问题的基础模型。几乎所有的神经网络在进行多分类任务时，最后一层都会接入Softmax层来输出各类的概率。

### (4) 随机森林 (Random Forest)
**原理**：由多棵决策树组成，每棵树在训练时都随机抽取一部分数据和特征。最后在预测时，综合所有决策树的结果，通过投票或求平均得出最终结论。
**使用场景**：在处理表格型数据时效果很好。抗噪能力强，不易过拟合，不需要复杂的调参就能取得不错的表现。

### (5) XGBoost
**原理**：XGBoost是串行构建决策树的，每一棵新树的目标是拟合前面所有树预测结果的残差，在模型精度和计算速度上都有很大的提升。
**使用场景**：在处理结构化的表格数据时表现很好。


## 2. 传统算法与神经网络的区别

传统算法很依赖特征提取。如果直接输入原始像素，由于包含太多背景和噪声，效果会很差。而神经网络能够自动学习并提取更高阶的特征，省去了繁琐的手工特征设计过程。
传统算法在小数据集上表现稳定，不易过拟合，神经网络对数据量和算力要求高，数据量越大其性能上限越高，也高度依赖GPU加速进行训练。
传统模型如随机森林，可以直观地评估出哪些特征对预测结果贡献最大。而神经网络由于参数量巨大且内部结构非线性，很难精确解释模型做出特定预测的具体逻辑。
