# 生成式 AI 的训练流程总结

## 1.自监督学习
在这个阶段，模型会阅读互联网上海量的无标注文本数据。模型的核心任务是预测下一个词，给定一段上文，让模型预测接下来最可能出现的一个词是什么。
经过大量的无监督训练，模型掌握了基础的语法、词汇以及世界知识，能够生成流畅的句子。不过，这时候模型还不知道怎么和人进行问答对话，只会顺着给出的话继续往下写。

## 2.监督式学习
人工构造一批高质量的“指令-回答”数据对，告诉模型在面对提问或特定指令时，应该以什么样的格式和内容来做出回应。把模型从一个单纯只会做文字预测的文本生成器，变成一个能够听懂人类指令并正确回答问题的对话助手。

## 3.人类反馈强化学习
针对同一个问题让模型给出多个不同的回答，让人类对这些回答进行好坏排序。然后用这些排序数据训练一个奖励模型，最后通过强化学习算法，让大语言模型不断朝着能获得更高奖励的方向调整参数。
